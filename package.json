{
  "name": "lomdim-lerega",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "heroku-prebuild": "echo 'starting prebuild script' && cd server && npm run dual-install",
    "build": "echo 'starting build script' && cd webClient && npm run build",
    "start": "echo 'starting start script' && cd server && npm start"
  },
  "engines": {
    "node": "20.x"
  },
  "author": "",
  "license": "ISC"
}
